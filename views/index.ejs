 
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>QuickLink | Node.js URL Shortener</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <style> body { font-family: 'Inter', sans-serif; } </style>
</head>
<body class="bg-slate-50 text-slate-800 flex items-center justify-center min-h-screen">
    <div class="w-full max-w-2xl mx-auto p-8 bg-white rounded-2xl shadow-lg text-center">
        <h1 class="text-4xl font-bold mb-2">QuickLink</h1>
        <p class="text-slate-500 mb-8">Your friendly, fast, and simple URL shortener.</p>

        <!-- The form sends a POST request to the /shorten endpoint -->
        <form action="/shorten" method="POST" class="flex flex-col sm:flex-row gap-4">
            <input type="url" name="longUrl" placeholder="https://your-super-long-url.com" required class="flex-grow p-4 text-lg border-2 border-slate-200 rounded-lg focus:ring-4 focus:ring-blue-200 focus:border-blue-500 focus:outline-none transition" />
            <button type="submit" class="bg-blue-600 text-white font-bold text-lg px-8 py-4 rounded-lg hover:bg-blue-700 focus:outline-none focus:ring-4 focus:ring-blue-300 transition">Shorten!</button>
        </form>

        <!-- This block will only display if the server sends back an error message -->
        <% if (locals.error) { %>
            <div class="mt-6 p-4 bg-red-100 text-red-700 rounded-lg"><%= error %></div>
        <% } %>

        <!-- This block will only display if the server sends back a successful shortUrl -->
        <% if (locals.shortUrl) { %>
            <div class="mt-8 p-6 bg-slate-100 rounded-lg">
                <p class="text-lg">Your shortened URL is ready:</p>
                <div class="mt-4 flex items-center justify-center gap-4 bg-white p-4 rounded-lg border">
                    <a href="<%= shortUrl %>" target="_blank" class="text-xl font-medium text-blue-600 hover:underline"><%= shortUrl %></a>
                </div>
            </div>
        <% } %>
    </div>
</body>
</html>

