<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=all">
    <title>QuickLink URL Shortener</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-900 text-white flex items-center justify-center min-h-screen font-sans">
    <div class="bg-slate-800 p-8 rounded-lg shadow-2xl w-full max-w-lg">
        <h1 class="text-4xl font-bold text-center mb-2 text-violet-400">QuickLink</h1>
        <p class="text-center text-slate-400 mb-8">Create short and memorable links in seconds.</p>
        
        <form action="/shorten" method="POST" class="space-y-4">
            <div>
                <label for="longUrl" class="block mb-2 text-sm font-medium text-slate-300">Enter Long URL:</label>
                <input 
                    type="url" 
                    id="longUrl" 
                    name="longUrl" 
                    placeholder="https://example.com/a-very-long-url-to-shorten" 
                    required
                    class="w-full p-3 bg-slate-700 border border-slate-600 rounded-md focus:ring-2 focus:ring-violet-500 focus:outline-none transition">
            </div>

            <!-- Optional Field for Custom Link -->
            <div>
                <label for="customCode" class="block mb-2 text-sm font-medium text-slate-300">Custom Name (Optional):</label>
                <div class="flex items-center">
                    <!-- THIS IS THE CORRECTED LINE -->
                    <span class="p-3 bg-slate-700 border border-slate-600 rounded-l-md text-slate-400"><%= baseUrl.replace('http://', '').replace('https://', '') %>/</span>
                    <input 
                        type="text" 
                        id="customCode" 
                        name="customCode" 
                        placeholder="my-resume"
                        class="w-full p-3 bg-slate-700 border border-slate-600 rounded-r-md focus:ring-2 focus:ring-violet-500 focus:outline-none transition">
                </div>
            </div>

            <button type="submit" class="w-full bg-violet-600 hover:bg-violet-700 text-white font-bold py-3 px-4 rounded-md transition duration-300">
                Generate Short Link
            </button>
        </form>

        <% if (error) { %>
            <div class="mt-4 bg-red-500/20 border border-red-500 text-red-300 p-3 rounded-md text-center">
                <%= error %>
            </div>
        <% } %>

        <% if (shortUrl) { %>
            <div class="mt-6 p-4 bg-slate-700 rounded-md text-center">
                <p class="text-slate-300">Your shortened link is ready:</p>
                <div class="flex items-center justify-center mt-2">
                    <a href="<%= shortUrl %>" target="_blank" class="text-xl font-mono text-violet-400 hover:underline"><%= shortUrl %></a>
                </div>
            </div>
        <% } %>
    </div>
</body>
</html>

